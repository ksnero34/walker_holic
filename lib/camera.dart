import 'package:flutter/cupertino.dart';

class camera extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Column(
        mainAxisAlignment: MainAxisAlignment.center,
        children: [Text('Camera Page'), Text('이선우 대머리')]);
  }
}
